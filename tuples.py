# tuples are immuteables means we can't change them
# it is represent by ()
numbers  = (1,2,3,3)
# numbers[1] = 4 gives error
numbers.count(3) # result => 2 

print(numbers.index(3)) # it will give index of 1st item function finds